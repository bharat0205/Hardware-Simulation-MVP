export default function DesignArea() {
  return (
    <div style={{
      flex: 1,
      background: "#fff",
      border: "2px dashed #e0e0e0",
      margin: "20px",
      borderRadius: 12,
      minHeight: 400,
      position: "relative"
    }}>
      {/* Drag and drop area */}
    </div>
  );
}
